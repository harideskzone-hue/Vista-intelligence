# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.100.0,<1.0
uvicorn>=0.22.0,<1.0
python-multipart>=0.0.6
starlette>=0.27.0

# ── Face Recognition (InsightFace / ONNX) ────────────────────────────────────
insightface>=0.7.3,<0.8
onnxruntime>=1.16.0,<2.0

# ── Vector Search ─────────────────────────────────────────────────────────────
faiss-cpu>=1.7.4,<2.0

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.24.0,<2.3
scipy>=1.13.0

# ── Border / Vehicle Detection (YOLO + ByteTrack) ────────────────────────────
# Used by: edge/boundary/adapter.py, edge/vehicle/adapter.py, face_engine/live_scorer.py
ultralytics>=8.3.0,<9.0

# ── Auth & Session ────────────────────────────────────────────────────────────
itsdangerous>=2.1.0
bcrypt>=4.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
pandas>=2.2.0
tqdm>=4.66.0

# ── Note: ffmpeg must be installed separately (system dependency) ─────────────
# macOS:  brew install ffmpeg
# Ubuntu: sudo apt install ffmpeg
# Used by: edge/events/clip_manager.py for H.264 browser-compatible clip encoding

# -- Remote Camera WebSocket support ----------------------------------------
websockets>=12.0              # AsyncIO WebSocket (server side — receives relay frames)
websocket-client>=1.7.0       # Sync WebSocket (camera_relay.py agent)
